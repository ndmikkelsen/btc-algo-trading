# =============================================================================
# Bybit API Credentials
# =============================================================================
# Get testnet keys at https://testnet.bybit.com -> API Management
BYBIT_API_KEY=your-testnet-api-key
BYBIT_API_SECRET=your-testnet-api-secret

# Use testnet (true) or mainnet (false). Default: true
# WARNING: Set to false only when you are ready for real trading!
BYBIT_TESTNET=true

# =============================================================================
# Trading Parameters (all optional -- defaults shown)
# =============================================================================
# Model: glft (Guéant-Lehalle-Fernandez-Tapia) or as (Avellaneda-Stoikov)
TRADING_MODEL=glft

# Fee tier: regular, vip1, vip2, market_maker
FEE_TIER=regular

# Kappa calibration: live (from order book) or constant
KAPPA_MODE=live

# Initial capital in USDT
INITIAL_CAPITAL=1000

# Order size in BTC
ORDER_SIZE=0.003

# Quote update interval in seconds
QUOTE_INTERVAL=5.0
