# Kamal 2 shared base config for btc-algo-trading infrastructure
#
# Destinations:
#   kamal deploy -d cognee    Cognee API + PostgreSQL/pgvector (semantic search)
#   kamal deploy -d dolt      Dolt SQL server (Beads backend)
#
# This base config is not deployed directly — always use a destination.

service: btc-algo-trading

image: btc-algo-trading/base

servers:
  - 10.10.20.138

registry:
  server: harbor.compute.lan
  username: admin
  password:
    - KAMAL_REGISTRY_PASSWORD

ssh:
  user: compute
  keys:
    - ~/.ssh/z3r0Layer-main

builder:
  arch: amd64
